\documentclass[
  12pt, % Устанавливает размер шрифта документа (12pt, 14pt)
  a4paper, % Лист А4
  simple, % Тоже самое что nocolumnsxix, nocolumnxxxi и nocolumnxxxii
  floatsection % Счётчик рисунков обнуляется каждую главу и нумеруется с номером главы, например, Рисунок 2.1
]{eskdtext}

\usepackage{config/env}
\usepackage{config/env.practice}
\usepackage{styles/listing}
\usepackage{styles/lists}
\usepackage{styles/SectionMargins}
\usepackage{styles/table}
\usepackage{styles/TableOfContent}
\usepackage{styles/url}

\def \envPracticeCode {ОТП.\envPracticeStudentGroupName\envPracticeStudentGroupNumber.\envPracticeStudentCard-0\envPracticeNumber~81~00}

\usepackage{fancybox, fancyhdr} 
\fancypagestyle{plain} % Колонтитул - код и номер страницы
{
  \renewcommand{\footrulewidth}{0pt}          % Толщина отделяющей полоски снизу
  \renewcommand{\headrulewidth}{0pt}          % Толщина отделяющей полоски сверху
  \fancyhead[C]{\hfill\envPracticeCode\hfill\thepage} % Сверху по центру выводить код
  \fancyfoot{}                                % Очистить нижний колонтитул
}

\begin{document}
  \input{include/practice_title_page_pz.tex}

  % = = = = = = = = = =
  \ESKDstyle{title}       % стиль страницы как у рамки
  \thispagestyle{plain}   % стиль страницы как у колонтитулов
  \pagestyle{plain}       % стиль страницы как у колонтитулов
  \hspace{0pt}
  % = = = = = = = = = =
  
  ...
  \newpage

  ...
\end{document}
